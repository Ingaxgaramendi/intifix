"""
Settings package for Intifix Admin.

The active settings module is selected via the DJANGO_SETTINGS_MODULE
environment variable, e.g.:

    intifix_admin.settings.development   (default)
    intifix_admin.settings.production

Importing this package directly does nothing on purpose: always point
DJANGO_SETTINGS_MODULE at a concrete environment module.
"""
