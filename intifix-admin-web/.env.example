# Base URL of the Django REST API. Leave empty in dev to use the Vite proxy
# (configured in vite.config.ts), or set it for a separate API origin in prod.
VITE_API_URL=

# Backend target used by the Vite dev proxy.
VITE_API_PROXY_TARGET=http://localhost:8000
